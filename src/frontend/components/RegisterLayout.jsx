import React from 'react';
import '../assets/styles/components/RegisterLayout.scss';

const RegisterLayout = ({ children }) => (
  <>
    {' '}
    {children}
  </>
);

export default RegisterLayout;
